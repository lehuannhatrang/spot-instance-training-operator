---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: spotinstancejobs.training.training.dcnlab.com
spec:
  group: training.training.dcnlab.com
  names:
    kind: SpotInstanceJob
    listKind: SpotInstanceJobList
    plural: spotinstancejobs
    singular: spotinstancejob
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SpotInstanceJob is the Schema for the spotinstancejobs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of SpotInstanceJob
            properties:
              checkpointConfig:
                description: CheckpointConfig defines checkpoint configuration
                properties:
                  checkpointImageRepo:
                    description: CheckpointImageRepo is the container registry repository
                      for checkpoint images
                    type: string
                  checkpointInterval:
                    description: CheckpointInterval is the interval between scheduled
                      checkpoints
                    type: string
                  checkpointStorage:
                    description: CheckpointStorage defines the storage configuration
                      for checkpoints
                    properties:
                      bucket:
                        description: Bucket name for checkpoint storage
                        type: string
                      pathPrefix:
                        description: Path prefix for checkpoint storage
                        type: string
                      type:
                        description: Type of storage (e.g., "gcs", "s3")
                        type: string
                    type: object
                required:
                - checkpointImageRepo
                - checkpointInterval
                type: object
              gcpCredentialsSecretRef:
                description: GCPCredentialsSecretRef is a reference to the secret
                  containing GCP credentials
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              jobTemplate:
                description: JobTemplate is the template for the Job to be created
                x-kubernetes-preserve-unknown-fields: true
              replicas:
                default: 2
                description: 'Replicas is the number of job replicas to maintain (default:
                  2)'
                format: int32
                minimum: 1
                type: integer
              spotInstanceVMRef:
                description: SpotInstanceVMRef is a reference to the SpotInstanceVM
                  resource
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - checkpointConfig
            - gcpCredentialsSecretRef
            - jobTemplate
            - spotInstanceVMRef
            type: object
          status:
            description: status defines the observed state of SpotInstanceJob
            properties:
              activeReplicas:
                description: ActiveReplicas is the number of actively running job
                  replicas
                format: int32
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                  of an object's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jobStatuses:
                description: JobStatuses tracks the status of each job replica
                items:
                  description: JobReplicaStatus tracks the status of a single job
                    replica
                  properties:
                    lastCheckpointImage:
                      description: LastCheckpointImage is the checkpoint image used
                        by this replica
                      type: string
                    name:
                      description: Name is the name of the job
                      type: string
                    nodeName:
                      description: NodeName is the node where the job is running
                      type: string
                    phase:
                      description: Phase is the current phase of the job replica
                      type: string
                  required:
                  - name
                  type: object
                type: array
              lastCheckpointImage:
                description: LastCheckpointImage is the image name of the last checkpoint
                type: string
              lastCheckpointTime:
                description: LastCheckpointTime is the last time a checkpoint was
                  created
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
